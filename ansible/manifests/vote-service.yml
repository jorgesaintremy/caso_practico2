apiVersion: v1
kind: Service
metadata:
    name: frontend
    namespace: cp2
spec:
    type: LoadBalancer
    ports:
    - protocol: TCP
      port: 80
      targetPort: 80
    selector:
        app: frontend
